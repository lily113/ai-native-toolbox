# CHANGELOG

> 只记"改变了什么能力"和"踩过什么坑"，不记琐碎改动。

## 2026-09-18 · 训练时间 / 组合级排序 / 卡片层级 / AI 教练排版 / 知识库重建

**训练记录**
- 新增 **训练时间（几点到几点，可选）**：滚轮 **07:00–23:00、15 分钟一档**，也可**手动输入精确时间**（`15:08`，支持 `15:08-16:30`）；**结束时间 ⇄ 时长双向联动**（自己训练按时长推算结束、由时间段反推时长；上课时长恒 = 节数 × 30，不一致时提示）；**跨零点**可算
- 同一日多条记录 **按时间段先后排序**（没填时间段的排最后）
- **「已选」区重做**：一行 = 一个组合、动作标题可点折叠、**↑ ↓ 按钮排序**（动作=整组移动 / 组合=组内移动 / 越界不越组）——替换掉不稳定的 `movable-view` 拖拽（固定行高定位导致卡片互相压盖、且经常拖不动）
- 训练笔记格式改为 **「【动作名】+ 　1. 组合」**，单组合合并一行；组合顺序跟随排序；**兼容并自动替换四代历史格式**（`· 名：a、b` / `· 名`+`　1. a` / `▎名`+`　- a` / 现格式）

**首页卡片**
- **展开 / 收起**：`展开全部 N 项 ▾` ↔ `收起 ▴`（按**练习项**计数，动作标题行不占预算，不会只剩一个孤立标题）
- **层级化渲染**：`▍动作名`（加粗 + 蓝色竖条）/ 组合带**序号列 + 悬挂缩进**（长文本换行不再顶到最左）/ 单组合合并一行且带竖条 / 考级标签用琥珀色区分

**AI 教练**
- 回答**结构化渲染**：标题、列表（有序/无序）、加粗、引用、分隔线、**真表格**（列数自适应、不丢列）
- **参考来源按文档去重合并**（显示页码范围与段数、可折叠；中译标「未核对」、旧赛季标「历史版本」）

**云函数知识库（重建）**
- **1237 块 / 约 108 万字符**，每块带 `discipline / doc / docNo / season / lang / official / verified / page / section`
- **来源升级为官方英文原文为主**：ISU Sports Rules 2026、TP Handbook 单人/双人 2026-27、Comm 2786（SOV）/2788（难度与 GOE）、冰舞 Comm 2795/2797/2798/2803 + ISU 官方中译 5 份 + 国家官方（《国家花样滑冰等级测试大纲》修改/修订公告、全国技术规则）+ 术语参考与个人笔记
- 检索：**中→英术语映射**（中文提问可命中英文原文）、覆盖率打分、**项目过滤**、**赛季隔离**、动作代码（2Lz/3T）与 **SOV 优先**、PDF 符号字体乱码（PUA）清理、表格用 `-layout` 抽取以保住行标签
- 提示词：**必须带出处**、中译须声明「未与原文核对」、**名称规范**（「国家花样滑冰等级测试」）、排版要求（表格 ≤3 列）、**用户画像**

**修复的 bug（本批，都是实测踩出来的）**
- 首页「展开」点了没反应 → `toggleCard` 调用了不存在的 `refresh()`（应为 `refreshDay()`）
- 组内拖拽**落到标题行**时静默不生效（标题行键不在排序键集合里）
- 老记录首页**顺序丢失** → `recordLines` 只认组合级键，不认动作级旧键 `m:<id>`
- 切到「上课」后**时长仍留 90 分钟**（与「2 节 × 30」矛盾）→ `syncTime` 未按节数推导
- 历史格式匹配**过宽会误删手写笔记**（`· 动作名` 这类裸匹配）→ 改为「整组一致」才认定
- `TIME_FROM/TIME_TO` **未定义** → 一选时间就 `ReferenceError`（语法检查查不出）
- `record.js` 存在**整段重复方法定义**（12 个方法各两份，后者覆盖前者）
- WXSS **重复规则**导致标题字号不一致；WXML 模板内使用 `.toFixed()` 导致**编译失败**

**姿态识别可行性评估（P0，结论：部分可做）**
- 用一段 720p/30fps 侧面训练视频实测（Apple Vision 人体关键点，与 iOS VisionKit 同源）：**检出率 96.2%**、主体占画面 38–43%、误检到他人仅 **1/308 帧**、主要关节置信度 0.64–0.76、帧间抖动中位 2.5–6.5%
- ✅ 可做：**浮足高度/两腿夹角**（两脚高度差中位 0.16 身高、90 分位 0.78，区分度极好）、**膝角与变化速率**、**上身前倾**、**单足保持时长**、节奏计时
- ❌ 不可做：**用刃（内/外刃）**——要判的是"3–4.7mm 厚薄板相对冰面的倾斜方向"，关键点模型到脚踝为止；侧视时倾斜方向垂直于画面；3m/1080p 下接触点偏移仅约 **2–3 像素**，叠加冰面过曝与运动模糊 → 只能给出"看似合理但错误"的反馈。跳跃高度（单目定机位无法解透视）与旋转圈数自动判定同样不可靠
- **决定**：视频**不作为训练记录的一部分**（不存储、不上传、零云成本），仅作为姿态识别的输入；识别走**端上**（VisionKit）

**工程**
- 新增静态体检脚本 `01-skating-coach/tools/audit_mp.py`：① wxml 绑定 ↔ 方法 ② `this.x()` 调用 ↔ 定义 ③ 方法重复定义 ④ 模板字段 ↔ data ⑤ 样式重复选择器 ⑥ 未定义常量引用 ⑦ WXML 模板内非法调用（本批 3 个 bug 就是靠它或它对应的检查抓出来的）

---

## 2026-09-15 · 上课形式 + 截图进仓库

**训练记录**
- 新增**上课形式**：一对一 / 一对二 / 一对多（仅「上课」记录可设；新建时**沿用上次选择**）
- 历史上课记录**一次性补标为一对一**——但不编造特殊值：一对二 / 一对多需要哪条就手动改哪条
- 首页卡片显示为「上冰 · 上课 · 2 节 · 一对一」；回顾页按月统计「上课形式：一对一 2 · 一对二 1」

**仓库与文档**
- 补齐 **9 张项目截图**（首页 / 记录 / 回顾 / 考级 / 动作 / AI 教练 / 设置 / PWA 桌面 / 里程碑），README 内嵌图片引用
- 新增 `CHANGELOG.md`；README 修正 3 处过期描述（语音录入已放弃、考级是「备考」不是「答题/题库」、回顾趋势图已实现）
- 截图体积优化：原始 3.5MB → 1.26MB（逐张选最优编码，文字仍锐利）

---

## 2026-09-14 · UI/UX 优化 + 六年数据迁移 + 节数体系

**训练记录**
- 新增**节数**：上课记录选 1/2/3/4 节，**时长自动 = 节数 × 30 分钟**；自己训练改为 60/90/120/150 分钟选择框
- 「训练内容」与「课后总结/备注」合并为**一个训练笔记框**（旧数据一次性自动并入，不丢）
- 勾选动作/组合/考级**不再覆盖手写笔记**（改为增量维护自动生成的行）
- 已选条目**按住 ≡ 拖动排序**，顺序同步到笔记文本与首页卡片
- 新增**动作搜索**（匹配动作名与组合名）+ 「**最近用过**」（最近 60 天，按最近时间排序）
- 笔记输入框改为**自适应高度**（最小 200px）

**首页**
- 「＋ 添加训练」改为**固定底部按钮**，不用再滚到底
- 记录卡片**只显示"练了什么"**（动作/组合/考级 + 节数），笔记留在记录里

**回顾页**
- 新增**近 12 个月训练次数柱状图**（当月亮蓝高亮）+ **本月类型占比条**
- 累计口径明确：**累计上冰 / 累计上冰课次数 / 累计节数**（只算上冰课，陆地课不进累计）

**考级备考（分包）**
- 分节**可折叠**；「规定步法（图案）」与「我的要点」默认展开，考纲长文默认收起
- 内置**步法图**（压缩到 155KB，走子包，不占主包）
- 修复"关联动作库动作选不了"（`showActionSheet` 选项上限 6 个 → 改用原生滚轮选择器）
- 「复制全文」改为「**复制我的易错**」（只导出个人要点，便于喂给 AI）

**数据安全**
- 导入前**自动备份**本机记录，设置里可「恢复导入前」
- `meta`（基线/每周目标）改为**取大值合并**——不会被 0 或缺失覆盖
- 考级数据**深合并**（按条目并集，不整条覆盖）
- **开发者工具不参与自动同步**（此前模拟器会把调试数据推上云端，导致真实数据被覆盖）
- 云端**历史快照 5 份 + 一键回滚**；上传前**体检**（本机明显更少则阻止）
- 修复首页卡片数据结构与模板不匹配导致的**空行/空点**问题

**设置页分层**
- 排查类工具（同步诊断、数据概览、多出的记录、恢复导入前、复制原始数据、合并重复记录）收进「🔧 高级 / 数据排查」折叠区，日常界面保持干净
- 移除一次性的"上课记录排查"对账工具

**数据迁移（六年记录）**
- 从纯文本迁入 **540 条训练记录**（2020-07-01 ~ 2026-07-02）+ **49 段课堂笔记**
- 剔除 **11 条非滑冰记录**（芭蕾 / 软开 / 基训 / 现代 / 身韵）；混合行保留原文
- 用笔记本里的累计编号做锚点**反推每次课的节数**：107 个锚点误差 **0**，导入后"累计节数 **271**"与纸笔记录完全一致

**工程**
- 修复**包体超限**：主包一度 2.6MB 上传失败（1.9MB 误入项目的头像图 + 600KB 扫描图）→ 图片压缩 + 考级页改**分包**，最终**主包 119KB / 子包 187KB**
- 新增自检脚本习惯：交付前跑「wxml 绑定 ↔ js 实现」「模板字段 ↔ data 声明」两项校验（这次确实靠它抓到过一个空白的节数选择控件）

**文档**
- README（顶层 / 项目 / 小程序）与截图清单同步刷新；修正三处过期描述：语音录入（已放弃，个人主体不能用官方插件）、考级分包（是"考级备考"不是"答题/题库"）、回顾趋势图（已实现）

---

## 2026-09-11 · 作品集仓库建立

- 归档 PWA 版与小程序版源码；README 记录云函数方案、数据隔离、知识库检索、双端互转、自动同步防覆盖等工程设计

---

## 早期里程碑

- **v2 微信小程序**：8 个主页面迁移（首页 / 训练 / 动作库 / 动作详情 / 回顾 / AI 教练 / 里程碑 / 设置）+ 云开发数据库同步 + DeepSeek 云函数 + 自动同步
- **v1 PWA**：单文件 `index.html` + Service Worker + localStorage，日历记录 / 动作库 / 回顾 / JSON 备份 / 离线可用；踩过 localStorage 丢数据、Service Worker 缓存、`file://` 下无法安装到主屏三个坑
